spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    database-platform: org.hibernate.dialect.H2Dialect

application:
  lastFm:
    url: http://ws.audioscrobbler.com
    apiKey: 6e4570bc243deec98dc89907217698de
